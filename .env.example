# Copy to .env and set a stable key shared by Deckhand Core and the OpenDeck plugin.
# Core reads DECKHAND_API_KEY from the environment or from [auth] in config.toml.
DECKHAND_API_KEY=your-write-key-here
DECKHAND_URL=http://127.0.0.1:8000
